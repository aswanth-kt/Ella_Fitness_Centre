import { ArrowUp } from 'lucide-react';
import { useEffect, useState } from 'react';
const BackToTop = () => { const [visible, setVisible] = useState(false); useEffect(() => { const update = () => setVisible(window.scrollY > 500); update(); window.addEventListener('scroll', update, { passive: true }); return () => window.removeEventListener('scroll', update); }, []); return visible ? <button type="button" onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })} className="fixed bottom-6 right-6 z-40 grid h-11 w-11 place-items-center rounded-full border border-rose-400/40 bg-rose-600 text-white shadow-lg shadow-rose-950/50 transition hover:-translate-y-1 hover:bg-rose-500 focus:outline-none focus:ring-2 focus:ring-rose-300" aria-label="Back to top"><ArrowUp className="h-5 w-5" /></button> : null; };
export default BackToTop;
